version: "2"

sql:
  - engine: "postgresql"
    schema: "db/migrations"
    queries: "db/query"
    gen:
      go:
        package: "repository"
        out: "internal/repository"
        sql_package: "database/sql"
